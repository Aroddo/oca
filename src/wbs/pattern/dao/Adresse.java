package wbs.pattern.dao;

// enitity
public class Adresse {

	// die klasse wird gemappt auf eine tabelle einer datenbank
	// die felder der klasse werden gemappt auf spalten einer tabelle.
	// instanzen der klasse werden gemappt auf die zeilen der tabelle.
}
